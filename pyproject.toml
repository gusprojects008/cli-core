[project]
name = "cli-core"
version = "1.0.0"
description = "Reusable CLI utilities"
authors = [{name = "Gustavo"}]
dependencies = ["rich"]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
